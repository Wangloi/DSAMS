## TODO

- [ ] Update `resources/js/pages/student/Dashboard.tsx` so “Request Slip” opens a modal (Dialog) on the student dashboard.
- [ ] Inline the admission-slip request form UI and submission logic in the modal (reuse same route `studentAdmissionSlipStore()` and fields).
- [ ] Ensure modal closes and form resets on success.
- [ ] Verify TypeScript build (or `npm run dev` compile) and manual behavior: click “Request Slip” -> modal opens; submit -> modal closes.
